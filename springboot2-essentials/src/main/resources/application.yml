

server:
  error:
    include-stacktrace: on_param


spring:
  datasource:
    url:
      jdbc:postgresql://localhost:5432/animesbd?useSSL=false&CreateDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

